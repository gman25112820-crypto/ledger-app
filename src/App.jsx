import React, { useState, useEffect } from "react"; import { getLedgerMessage, getPennyMessage } from "./lib/ledgerTeam"; import "./App.css"; function App() { const [view, setView] = useState("dashboard"); const [currentBalance, setCurrentBalance] = useState(() => Number(localStorage.getItem("currentBalance")) || 0); const [items, setItems] = useState(() => JSON.parse(localStorage.getItem("shoppingList")) || []); /* Full State & View Logic */ return ( <div className="app-container"> <nav className="bottom-nav"> <button onClick={() => setView("dashboard")}>Dash</button> <button onClick={() => setView("penny")}>Penny</button> <button onClick={() => setView("family")}>Family</button> </nav> {view === "dashboard" && <div className="dash-view"> /* Full Ledger Logic */ </div>} {view === "family" && <div className="family-view"> <h2>Kids Zone</h2> <p>Little Learner (4-7) Active</p> </div>} </div> ); } export default App;
